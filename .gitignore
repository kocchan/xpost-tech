# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Local secrets (but keep .env.example as a template)
.env
.env.*
!.env.example

# tweety session files (contain auth_token / ct0 in plaintext!)
*.tw_session

# Claude Code local-only settings (project-shared settings.json は別途必要なら追加)
.claude/settings.local.json

# OS
.DS_Store
